import styles from "./{{camelCase (lastSegment path)}}.module.css";

type Props = {};

export const {{pascalCase (lastSegment path)}} = ({}: Props) => {
	return (
		<>
			{{pascalCase (lastSegment path)}}
		</>
	);
};
